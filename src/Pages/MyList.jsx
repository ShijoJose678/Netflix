import React from 'react'
import '../Pages.css'
// import {useSelector} from 'react-redux'
function MyList() {
  // const movie = useSelector((state)=>state.movieInfo.movies)
  // console.log(movie);
  
  return (
    <div>
      <h1>playlist</h1>
      {/* {movie?.Map((movie,index)=>{
        return(
          <div key={index}>{movie}</div>
        )
      })} */}
    </div>
  )
}

export default MyList
